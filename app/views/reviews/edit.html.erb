<h2> Edit review</h2>

<%= form_for @review do |f| %>

<p><%= f.label :body %> </br>
<%= f.text_area :body %> 
</p>
<%= f.label :rating %>
<%= f.select :rating, (1..5).to_a%> 
<%= f.submit %>
<%end %>